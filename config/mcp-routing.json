{
  "$schema": "./mcp-routing.schema.json",
  "routing": {
    "client": [],
    "server": []
  },
  "default": "client",
  "_comments": {
    "client": "Runs on user's machine - all tools execute locally for security/privacy",
    "server": "Empty - no server-side execution",
    "default": "All tools route to client (local execution)"
  }
}
