import { describe, expect, it } from 'vitest';

// NOTE: StorageAgent supprimé du codebase. Test legacy neutralisé pour éviter les erreurs de résolution de modules.
describe.skip('StorageAgent (legacy)', () => {
  it('tests skipped (agent removed)', () => {
    expect(true).toBe(true);
  });
});
