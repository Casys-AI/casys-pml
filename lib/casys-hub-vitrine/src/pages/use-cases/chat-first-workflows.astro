---
import LandingLayout from '../../layouts/LandingLayout.astro';
import UseCaseDetail from '../../features/landing-v2/UseCaseDetail.astro';
import { useCases } from '../../content/use-cases';

const uc = useCases.find(c => c.id === 'chat-first-workflows')!;
---

<LandingLayout title={`${uc.title.en} — Casys AI`} description="How a coaching startup replaced 5 scattered apps with one chat-first interface using MCP Apps. From dashboard fatigue to conversational workflows.">
  <UseCaseDetail useCase={uc} />
</LandingLayout>
