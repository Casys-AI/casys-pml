---
import LandingLayout from '../../../layouts/LandingLayout.astro';
import UseCaseDetail from '../../../features/landing-v2/UseCaseDetail.astro';
import { useCases } from '../../../content/use-cases';

const uc = useCases.find(c => c.id === 'plannable-mcp-workflows')!;
---

<LandingLayout title={`${uc.title.fr} — Casys AI`} description="Des pipelines statiques LangGraph aux workflows dynamiques MCP Engine avec Human-in-the-Loop intégré. Cas d'usage IA industrielle.">
  <UseCaseDetail useCase={uc} />
</LandingLayout>
