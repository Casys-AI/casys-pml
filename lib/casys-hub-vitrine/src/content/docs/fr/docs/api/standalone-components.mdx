---
title: Composants Standalone
description: Utiliser RateLimiter, RequestQueue, et SchemaValidator independamment.
next: false
---

import { Aside } from '@astrojs/starlight/components';

Les briques de base de `@casys/mcp-server` sont utiles seules. Rate limiting, controle de concurrence, et validation de schema — tout utilisable dans n'importe quel projet Deno ou Node.js, sans MCP.

## RateLimiter

Rate limiter a fenetre glissante avec suivi par cle. Leger, sans dependance externe.

```typescript
import { RateLimiter } from "@casys/mcp-server";

const limiter = new RateLimiter({
  maxRequests: 10,   // 10 requests
  windowMs: 1000,    // per 1 second
});
```

### checkLimit

```typescript
limiter.checkLimit(key: string): boolean;
```

Retourne `true` si dans les limites, `false` si rate-limited. Non-bloquant — n'attend jamais.

```typescript
if (limiter.checkLimit("client-123")) {
  // Process request
} else {
  // Rate limited
}
```

### waitForSlot

```typescript
await limiter.waitForSlot(key: string): Promise<void>;
```

Bloque jusqu'a ce qu'un slot se libere. Le timeout est egal a `windowMs`.

```typescript
await limiter.waitForSlot("client-123");
// Guaranteed to have a slot
```

### getMetrics

```typescript
limiter.getMetrics(): { keys: number, totalRequests: number };
```

## RequestQueue

Controle de concurrence avec backpressure configurable. Protege les services en aval de la surcharge.

```typescript
import { RequestQueue } from "@casys/mcp-server";

const queue = new RequestQueue({
  maxConcurrent: 5,
  strategy: "queue",     // "sleep" | "queue" | "reject"
  sleepMs: 10,           // For "sleep" strategy only
});
```

### Strategies

| Strategie | Comportement | Ideal pour |
|-----------|-------------|------------|
| `sleep` | Attente active avec delai `sleepMs` jusqu'a liberation d'un slot | Faible latence, tolerant au CPU |
| `queue` | File FIFO — les requetes attendent dans l'ordre | Ordonnancement equitable, previsible |
| `reject` | Rejet immediat si a pleine capacite | Fail-fast, style circuit-breaker |

### acquire / release

```typescript
await queue.acquire(): Promise<void>;
queue.release(): void;
```

Toujours utiliser try/finally pour eviter les fuites de slots :

```typescript
await queue.acquire();
try {
  await processRequest();  // 1 of maxConcurrent
} finally {
  queue.release();
}
```

### getMetrics

```typescript
queue.getMetrics(): { inFlight: number, queued: number };
```

## SchemaValidator

Validation JSON Schema propulsee par ajv. Les schemas sont compiles une seule fois a l'enregistrement — la validation est rapide.

```typescript
import { SchemaValidator } from "@casys/mcp-server";

const validator = new SchemaValidator();
```

### addSchema

```typescript
validator.addSchema(toolName: string, schema: object): void;
```

Compile et met en cache le schema. A appeler une fois par outil au demarrage :

```typescript
validator.addSchema("my_tool", {
  type: "object",
  properties: {
    count: { type: "number", minimum: 0 },
    label: { type: "string", maxLength: 100 },
  },
  required: ["count"],
});
```

### validate

```typescript
validator.validate(toolName: string, args: unknown): ValidationResult;
```

```typescript
interface ValidationResult {
  valid: boolean;
  errors: string[];  // Human-readable error messages
}
```

```typescript
validator.validate("my_tool", { count: -1 });
// { valid: false, errors: ["count must be >= 0"] }

validator.validate("my_tool", { count: 5 });
// { valid: true, errors: [] }
```

<Aside type="tip">
  Dans `ConcurrentMCPServer`, ces composants sont cables automatiquement selon vos options. L'utilisation standalone n'est necessaire que pour construire une infrastructure personnalisee en dehors du framework.
</Aside>

## Voir aussi

- [API ConcurrentMCPServer](/fr/docs/api/concurrent-mcp-server/) — Comment ces composants sont utilises en interne
- [Pipeline Middleware](/fr/docs/guides/middleware/) — Rate limiting, validation, et backpressure dans le pipeline
