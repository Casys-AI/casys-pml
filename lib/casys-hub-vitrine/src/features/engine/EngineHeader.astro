---
const locale = (Astro as any).currentLocale ?? 'en';
const translations: Record<string, Record<string, string>> = {
  en: {
    howItWorks: 'How It Works',
    shgat: 'SHGAT',
    benchmarks: 'Benchmarks',
    links: 'Links',
    docs: 'Docs',
  },
  fr: {
    howItWorks: 'Fonctionnement',
    shgat: 'SHGAT',
    benchmarks: 'Metriques',
    links: 'Liens',
    docs: 'Docs',
  },
  zh: {
    howItWorks: 'How It Works',
    shgat: 'SHGAT',
    benchmarks: 'Benchmarks',
    links: 'Links',
    docs: 'Docs',
  },
};
const t = translations[locale] ?? translations.en;
---

<header class="eng-header">
  <div class="eng-header-container">
    <a href="#eng-hero" class="eng-logo">
      <span class="eng-logo-pkg">casys/</span><span class="eng-logo-name">engine</span>
      <span class="eng-logo-version">v0.1</span>
    </a>

    <nav class="eng-nav-desktop">
      <div class="eng-nav-links">
        <a href="#how-it-works" class="eng-nav-link">{t.howItWorks}</a>
        <a href="#shgat" class="eng-nav-link">{t.shgat}</a>
        <a href="#benchmarks" class="eng-nav-link">{t.benchmarks}</a>
        <a href="#links" class="eng-nav-link">{t.links}</a>
        <a href="/docs/shgat/introduction/" class="eng-nav-link eng-nav-docs">
          <span class="material-symbols-rounded" style="font-size:16px">description</span>
          {t.docs}
        </a>
      </div>
      <div class="eng-nav-actions">
        <a href="https://github.com/Casys-AI/shgat" class="eng-gh-link" target="_blank" rel="noopener noreferrer" aria-label="GitHub">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/>
          </svg>
        </a>
        <a href="https://jsr.io/@casys/shgat" class="eng-jsr-btn" target="_blank" rel="noopener noreferrer">
          <span class="eng-jsr-text">JSR</span>
          <span class="material-symbols-rounded">arrow_outward</span>
        </a>
      </div>
    </nav>

    <button id="eng-mobile-menu-btn" class="eng-menu-btn" aria-label="Toggle menu" aria-expanded="false">
      <span class="eng-menu-bar"></span>
      <span class="eng-menu-bar"></span>
    </button>
  </div>
</header>

<div id="eng-mobile-menu" class="eng-mobile-menu">
  <nav class="eng-mobile-nav">
    <a href="#how-it-works" class="eng-nav-link">{t.howItWorks}</a>
    <a href="#shgat" class="eng-nav-link">{t.shgat}</a>
    <a href="#benchmarks" class="eng-nav-link">{t.benchmarks}</a>
    <a href="#links" class="eng-nav-link">{t.links}</a>
    <a href="/docs/shgat/introduction/" class="eng-nav-link">
      <span class="material-symbols-rounded" style="font-size:16px;vertical-align:-2px">description</span>
      {t.docs}
    </a>
    <div class="eng-mobile-cta">
      <a href="https://github.com/Casys-AI/shgat" class="eng-mobile-gh" target="_blank" rel="noopener noreferrer">
        GitHub
        <span class="material-symbols-rounded">arrow_outward</span>
      </a>
      <a href="https://jsr.io/@casys/shgat" class="eng-jsr-btn" target="_blank" rel="noopener noreferrer">
        <span class="eng-jsr-text">JSR</span>
        <span class="material-symbols-rounded">arrow_outward</span>
      </a>
    </div>
  </nav>
</div>

<style>
  .eng-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(0, 0, 0, 0.06);
    transition: transform 250ms ease-out;
  }

  .eng-header.header-hidden {
    transform: translateY(-100%);
  }

  .eng-header-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  /* Logo */
  .eng-logo {
    display: flex;
    align-items: baseline;
    gap: 0;
    text-decoration: none;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.875rem;
    font-weight: 500;
  }

  .eng-logo-pkg {
    color: var(--md-sys-color-on-surface-variant);
  }

  .eng-logo-name {
    color: var(--md-sys-color-on-surface);
    font-weight: 700;
  }

  .eng-logo-version {
    margin-left: 8px;
    font-size: 0.6875rem;
    font-weight: 600;
    color: var(--md-sys-color-primary);
    background: var(--md-sys-color-primary-container);
    padding: 2px 7px;
    border-radius: 100px;
  }

  /* Desktop Nav */
  .eng-nav-desktop {
    display: none;
    align-items: center;
    gap: 24px;
  }

  @media (min-width: 768px) {
    .eng-nav-desktop { display: flex; }
  }

  .eng-nav-links {
    display: flex;
    align-items: center;
    gap: 2px;
  }

  .eng-nav-link {
    position: relative;
    padding: 6px 12px;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--md-sys-color-on-surface-variant);
    text-decoration: none;
    border-radius: 6px;
    transition: color 150ms ease-out;
  }

  .eng-nav-link::after {
    content: '';
    position: absolute;
    bottom: 2px;
    left: 12px;
    right: 12px;
    height: 2px;
    background: var(--md-sys-color-primary);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 200ms ease-out;
  }

  .eng-nav-link:hover { color: var(--md-sys-color-on-surface); }
  .eng-nav-link:hover::after,
  .eng-nav-link.active::after { transform: scaleX(1); transform-origin: left; }
  .eng-nav-link.active { color: var(--md-sys-color-primary); font-weight: 600; }

  .eng-nav-docs { display: inline-flex; align-items: center; gap: 4px; }

  .eng-nav-actions {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-left: 8px;
    padding-left: 16px;
    border-left: 1px solid var(--md-sys-color-outline-variant);
  }

  .eng-gh-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    color: var(--md-sys-color-on-surface-variant);
    border-radius: 8px;
    transition: background 150ms ease-out, color 150ms ease-out;
  }

  .eng-gh-link:hover {
    background: var(--md-sys-color-surface-container);
    color: var(--md-sys-color-on-surface);
  }

  .eng-jsr-btn {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 6px 14px;
    background: var(--md-sys-color-primary);
    color: var(--md-sys-color-on-primary);
    font-size: 0.8125rem;
    font-weight: 700;
    text-decoration: none;
    border-radius: 8px;
    transition: background 150ms ease-out;
  }

  .eng-jsr-btn:hover { background: var(--md-sys-color-primary-container); color: var(--md-sys-color-on-primary-container); }
  .eng-jsr-btn .material-symbols-rounded { font-size: 16px; }

  .eng-jsr-text {
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 0.04em;
  }

  /* Mobile button */
  .eng-menu-btn {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 40px;
    height: 40px;
    padding: 10px;
    border: 1px solid rgba(0, 0, 0, 0.1);
    background: transparent;
    cursor: pointer;
    border-radius: 8px;
  }

  .eng-menu-bar {
    display: block;
    width: 100%;
    height: 2px;
    background: var(--md-sys-color-on-surface);
    border-radius: 1px;
    transition: transform 200ms ease-out;
  }

  .eng-menu-btn[aria-expanded="true"] .eng-menu-bar:first-child { transform: rotate(45deg) translateY(5px); }
  .eng-menu-btn[aria-expanded="true"] .eng-menu-bar:last-child { transform: rotate(-45deg) translateY(-5px); }

  @media (min-width: 768px) {
    .eng-menu-btn { display: none; }
  }

  /* Mobile menu */
  .eng-mobile-menu {
    position: fixed;
    top: 56px;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(20px);
    transform: translateX(100%);
    transition: transform 250ms ease-out;
    z-index: 99;
    overflow-y: auto;
  }

  .eng-mobile-menu.open { transform: translateX(0); }

  @media (min-width: 768px) {
    .eng-mobile-menu { display: none; }
  }

  .eng-mobile-nav {
    display: flex;
    flex-direction: column;
    padding: 24px;
    gap: 4px;
  }

  .eng-mobile-nav .eng-nav-link {
    padding: 14px 0;
    font-size: 1.125rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.06);
    border-radius: 0;
  }

  .eng-mobile-nav .eng-nav-link::after { display: none; }

  .eng-mobile-cta {
    display: flex;
    gap: 12px;
    margin-top: 20px;
    padding-top: 20px;
    border-top: 1px solid rgba(0, 0, 0, 0.08);
  }

  .eng-mobile-gh {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 12px;
    font-size: 0.875rem;
    font-weight: 600;
    text-decoration: none;
    color: var(--md-sys-color-on-surface);
    border: 1px solid var(--md-sys-color-outline-variant);
    border-radius: 8px;
  }

  .eng-mobile-gh .material-symbols-rounded { font-size: 16px; }

  .eng-mobile-cta .eng-jsr-btn {
    flex: 1;
    justify-content: center;
    padding: 12px;
  }
</style>

<style is:global>
  /* Engine Header - Dark Mode */
  html[data-theme="dark"] .eng-header {
    background: rgba(0, 0, 0, 0.85) !important;
    border-bottom-color: rgba(255, 184, 111, 0.08);
  }

  html[data-theme="dark"] .eng-logo-pkg { color: #a89a8c; }
  html[data-theme="dark"] .eng-logo-name { color: #f5f0ea; }
  html[data-theme="dark"] .eng-logo-version { background: rgba(255, 184, 111, 0.12); color: #FFB86F; }

  html[data-theme="dark"] .eng-nav-link { color: #d5c3b5; }
  html[data-theme="dark"] .eng-nav-link:hover { color: #f5f0ea; }
  html[data-theme="dark"] .eng-nav-link::after { background: #FFB86F; }
  html[data-theme="dark"] .eng-nav-link.active { color: #FFB86F; }

  html[data-theme="dark"] .eng-nav-actions { border-left-color: rgba(255, 184, 111, 0.12); }
  html[data-theme="dark"] .eng-gh-link { color: #d5c3b5; }
  html[data-theme="dark"] .eng-gh-link:hover { background: rgba(255, 184, 111, 0.1); color: #FFB86F; }

  html[data-theme="dark"] .eng-jsr-btn { background: #FFB86F; color: #1a1815; }
  html[data-theme="dark"] .eng-jsr-btn:hover { background: #D4A574; }

  html[data-theme="dark"] .eng-menu-btn { border-color: rgba(255, 184, 111, 0.08); }
  html[data-theme="dark"] .eng-menu-bar { background: #f5f0ea; }

  html[data-theme="dark"] .eng-mobile-menu { background: rgba(26, 24, 21, 0.98) !important; }
  html[data-theme="dark"] .eng-mobile-nav .eng-nav-link { border-bottom-color: rgba(255, 184, 111, 0.06); }
  html[data-theme="dark"] .eng-mobile-cta { border-top-color: rgba(255, 184, 111, 0.08); }
  html[data-theme="dark"] .eng-mobile-gh { color: #d5c3b5; border-color: rgba(255, 184, 111, 0.15); }
  html[data-theme="dark"] .eng-mobile-gh:hover { background: rgba(255, 184, 111, 0.08); color: #f5f0ea; }
</style>

<script>
  // Scroll hide/show
  const engHeader = document.querySelector('.eng-header');
  let engLastScrollY = window.scrollY;

  window.addEventListener('scroll', () => {
    const y = window.scrollY;
    if (y < 80) {
      engHeader?.classList.remove('header-hidden');
    } else if (y > engLastScrollY && y > 80) {
      engHeader?.classList.add('header-hidden');
    } else if (y < engLastScrollY) {
      engHeader?.classList.remove('header-hidden');
    }
    engLastScrollY = y;
  });

  // Mobile menu toggle
  const engMenuBtn = document.getElementById('eng-mobile-menu-btn');
  const engMobileMenu = document.getElementById('eng-mobile-menu');

  engMenuBtn?.addEventListener('click', () => {
    const isOpen = engMobileMenu?.classList.toggle('open');
    engMenuBtn?.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
  });

  engMobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      engMobileMenu.classList.remove('open');
      engMenuBtn?.setAttribute('aria-expanded', 'false');
    });
  });

  document.addEventListener('click', (e) => {
    const target = e.target as Node;
    if (!engMobileMenu?.contains(target) && !engMenuBtn?.contains(target) && engMobileMenu?.classList.contains('open')) {
      engMobileMenu.classList.remove('open');
      engMenuBtn?.setAttribute('aria-expanded', 'false');
    }
  });

  // Active section highlight
  const engSections = document.querySelectorAll('section[id]');
  const engNavLinks = document.querySelectorAll('.eng-nav-link[href^="#"]');

  const engObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const id = entry.target.id;
        engNavLinks.forEach((link) => {
          link.classList.toggle('active', link.getAttribute('href') === `#${id}`);
        });
      }
    });
  }, { threshold: 0.3, rootMargin: '-80px 0px -60% 0px' });

  engSections.forEach((s) => engObserver.observe(s));
</script>
