---
const locale = (Astro as any).currentLocale ?? 'en';

const translations = {
  en: {
    title: 'Ready to Get Started?',
    subtitle: 'Join the Casys PML waitlist, book a consulting call, or request training. 24h response time.',
    namePlaceholder: 'Your name',
    emailPlaceholder: 'Your work email',
    messagePlaceholder: 'I want to join the waitlist / book a consulting call / request training',
    submitButton: 'Send my request',
    sending: 'Sending...',
    successMessage: 'Request received! We\'ll get back to you within 24h.',
    errorMessage: 'Error sending message. Please try again.',
    hiddenSubject: 'New CASYS contact request'
  },
  fr: {
    title: 'Prêt à Démarrer?',
    subtitle: 'Rejoignez la waitlist Casys PML, réservez un appel consulting, ou demandez une formation. Réponse sous 24h.',
    namePlaceholder: 'Votre nom',
    emailPlaceholder: 'Votre email professionnel',
    messagePlaceholder: 'Je veux rejoindre la waitlist / réserver un appel consulting / demander une formation',
    submitButton: 'Envoyer ma demande',
    sending: 'Envoi...',
    successMessage: 'Demande reçue ! On revient vers vous sous 24h maximum.',
    errorMessage: 'Erreur lors de l\'envoi. Veuillez réessayer.',
    hiddenSubject: 'Nouvelle demande de contact CASYS'
  },
  zh: {
    title: '准备好开始了吗？',
    subtitle: '加入 Casys PML 等候名单、预约咨询电话或申请培训。24小时内回复。',
    namePlaceholder: '您的姓名',
    emailPlaceholder: '您的工作邮箱',
    messagePlaceholder: '我想加入等候名单 / 预约咨询 / 申请培训',
    submitButton: '发送请求',
    sending: '发送中...',
    successMessage: '已收到您的请求！我们将在24小时内回复。',
    errorMessage: '发送失败，请重试。',
    hiddenSubject: '新的 CASYS 联系请求'
  }
};
const t = translations[locale] ?? translations.en;
---

<section id="contact">
  <div class="container">
    <div class="section-header">
      <h2>{t.title}</h2>
      <p class="section-subtitle">{t.subtitle}</p>
    </div>

    <form
      id="contact-form"
      class="contact-form"
      action="https://formspree.io/f/movnynen"
      method="POST"
    >
      <div class="form-grid">
        <div class="form-group">
          <label for="contact-name" class="label">
            <span class="material-symbols-rounded">person</span>
            {t.namePlaceholder}
          </label>
          <input
            id="contact-name"
            type="text"
            name="name"
            placeholder={t.namePlaceholder}
            class="input"
            required
          />
        </div>

        <div class="form-group">
          <label for="contact-email" class="label">
            <span class="material-symbols-rounded">email</span>
            {t.emailPlaceholder}
          </label>
          <input
            id="contact-email"
            type="email"
            name="email"
            placeholder={t.emailPlaceholder}
            class="input"
            required
          />
        </div>
      </div>

      <!-- Hidden subject field with default value -->
      <input
        type="hidden"
        name="_subject"
        value={t.hiddenSubject}
      />

      <div class="form-group">
        <label for="contact-message" class="label">
          <span class="material-symbols-rounded">chat</span>
          {t.messagePlaceholder}
        </label>
        <textarea
          id="contact-message"
          name="message"
          placeholder={t.messagePlaceholder}
          class="textarea"
          rows="6"
          required
        ></textarea>
      </div>

      <button type="submit" class="submit-btn">
        <span class="btn-text">{t.submitButton}</span>
        <span class="btn-loading">{t.sending}</span>
        <span class="material-symbols-rounded">send</span>
      </button>

      <p id="contact-feedback" class="feedback" role="status"></p>
    </form>
  </div>
</section>

<style>
  /* ========================================
     CONTACT - Editorial/Refined Aesthetic
     Font: Fraunces for headlines
     Palette: Warm orange for dark mode
     ======================================== */

  section {
    padding: 80px 0;
    background: var(--md-sys-color-surface);
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* Left-aligned header - editorial style */
  .section-header {
    text-align: left;
    margin-bottom: 48px;
    max-width: 600px;
  }

  h2 {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 2.5rem;
    font-weight: 600;
    font-optical-sizing: auto;
    color: var(--md-sys-color-on-surface);
    margin: 0 0 12px;
    letter-spacing: -0.02em;
  }

  .section-subtitle {
    font-size: 1.1rem;
    color: var(--md-sys-color-on-surface-variant);
    margin: 0;
    line-height: 1.6;
  }

  /* Form */
  .contact-form {
    background: var(--md-sys-color-surface-container-lowest);
    border: 1px solid var(--md-sys-color-outline-variant);
    border-radius: 16px;
    padding: 40px;
  }

  @media (max-width: 640px) {
    .contact-form {
      padding: 24px;
    }
  }

  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-bottom: 24px;
  }

  @media (max-width: 640px) {
    .form-grid {
      grid-template-columns: 1fr;
    }
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .label {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--md-sys-color-on-surface);
  }

  .label .material-symbols-rounded {
    font-size: 18px;
    color: var(--md-sys-color-primary);
  }

  /* Input Styles */
  .input,
  .textarea {
    width: 100%;
    padding: 14px 16px;
    font-size: 1rem;
    font-family: inherit;
    color: var(--md-sys-color-on-surface);
    background: var(--md-sys-color-surface-container-high);
    border: 1px solid var(--md-sys-color-outline-variant);
    border-radius: 12px;
    outline: none;
    transition: border-color 200ms ease-out, box-shadow 200ms ease-out;
  }

  .input:focus,
  .textarea:focus {
    border-color: var(--md-sys-color-primary);
    box-shadow: 0 0 0 3px rgba(255, 153, 0, 0.1);
  }

  .input::placeholder,
  .textarea::placeholder {
    color: var(--md-sys-color-on-surface-variant);
    opacity: 0.6;
  }

  .textarea {
    resize: vertical;
    min-height: 140px;
  }

  /* Submit Button */
  .submit-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
    padding: 16px 32px;
    margin-top: 24px;
    font-size: 1rem;
    font-weight: 600;
    color: var(--md-sys-color-on-primary);
    background: var(--md-sys-color-primary);
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: background 200ms ease-out;
  }

  .submit-btn:hover:not(:disabled) {
    background: var(--md-sys-color-primary-container);
    color: var(--md-sys-color-on-primary-container);
  }

  .submit-btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .btn-loading {
    display: none;
  }

  .submit-btn.loading .btn-text {
    display: none;
  }

  .submit-btn.loading .btn-loading {
    display: inline;
  }

  .submit-btn .material-symbols-rounded {
    font-size: 20px;
  }

  /* Feedback Message */
  .feedback {
    margin-top: 16px;
    padding: 12px 16px;
    font-size: 0.95rem;
    text-align: center;
    border-radius: 8px;
    opacity: 0;
    transition: opacity 200ms ease-out;
  }

  .feedback.visible {
    opacity: 1;
  }

  .feedback.success {
    color: var(--md-sys-color-on-tertiary-container);
    background: var(--md-sys-color-tertiary-container);
  }

  .feedback.error {
    color: var(--md-sys-color-on-error-container);
    background: var(--md-sys-color-error-container);
  }

  /* Mobile Responsiveness */
  @media (max-width: 768px) {
    section {
      padding: 60px 0;
    }

    h2 {
      font-size: 2rem;
    }
  }
</style>

<style is:global>
  /* ========================================
     CONTACT - DARK MODE
     Warm orange palette (#FFB86F, #D4A574)
     ======================================== */

  html[data-theme="dark"] #contact {
    background: #0a0908;
  }

  html[data-theme="dark"] #contact h2 {
    color: #f5f0ea;
  }

  html[data-theme="dark"] #contact .section-subtitle {
    color: #d5c3b5;
  }

  /* Form */
  html[data-theme="dark"] #contact .contact-form {
    background: #1a1815;
    border-color: rgba(255, 184, 111, 0.1);
  }

  html[data-theme="dark"] #contact .label {
    color: #f5f0ea;
  }

  html[data-theme="dark"] #contact .label .material-symbols-rounded {
    color: #FFB86F;
  }

  /* Inputs */
  html[data-theme="dark"] #contact .input,
  html[data-theme="dark"] #contact .textarea {
    background: rgba(255, 184, 111, 0.05);
    border-color: rgba(255, 184, 111, 0.15);
    color: #f5f0ea;
  }

  html[data-theme="dark"] #contact .input:focus,
  html[data-theme="dark"] #contact .textarea:focus {
    border-color: #FFB86F;
    box-shadow: 0 0 0 3px rgba(255, 184, 111, 0.1);
  }

  html[data-theme="dark"] #contact .input::placeholder,
  html[data-theme="dark"] #contact .textarea::placeholder {
    color: #d5c3b5;
  }

  /* Submit Button */
  html[data-theme="dark"] #contact .submit-btn {
    background: #FFB86F;
    color: #1a1815;
  }

  html[data-theme="dark"] #contact .submit-btn:hover:not(:disabled) {
    background: #D4A574;
  }

  /* Feedback */
  html[data-theme="dark"] #contact .feedback.success {
    background: rgba(255, 184, 111, 0.15);
    color: #f5f0ea;
    border: 1px solid rgba(255, 184, 111, 0.2);
  }

  html[data-theme="dark"] #contact .feedback.error {
    background: rgba(255, 100, 100, 0.15);
    color: #ffcccc;
    border: 1px solid rgba(255, 100, 100, 0.2);
  }
</style>

<script>
  const contactForm = document.getElementById('contact-form') as HTMLFormElement | null;
  const contactFeedback = document.getElementById('contact-feedback') as HTMLParagraphElement | null;

  contactForm?.addEventListener('submit', async (e) => {
    e.preventDefault();

    if (!contactForm || !contactFeedback) return;

    const formData = new FormData(contactForm);
    const name = formData.get('name') as string;
    const email = formData.get('email') as string;
    const message = formData.get('message') as string;
    const subject = formData.get('_subject') as string;

    // Validation
    if (!name || !email || !message) return;

    const submitBtn = contactForm.querySelector('.submit-btn') as HTMLButtonElement;
    submitBtn.disabled = true;
    submitBtn.classList.add('loading');

    contactFeedback.classList.remove('visible', 'success', 'error');
    const lang = document.documentElement.lang;

    try {
      const response = await fetch('https://formspree.io/f/movnynen', {
        method: 'POST',
        body: JSON.stringify({ name, email, message, _subject: subject }),
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json'
        }
      });

      const data = await response.json();

      if (!response.ok) {
        throw new Error(data.error || 'Submission failed');
      }

      // Success
      contactFeedback.textContent = lang === 'fr'
        ? 'Message envoyé ! Nous vous répondrons rapidement.'
        : lang === 'zh'
        ? '消息已发送！我们会尽快回复。'
        : 'Message sent! We\'ll reply soon.';
      contactFeedback.classList.add('visible', 'success');

      // Reset form
      contactForm.reset();

      // Hide success message after 5 seconds
      setTimeout(() => {
        contactFeedback.classList.remove('visible');
      }, 5000);

    } catch (error) {
      // Error
      contactFeedback.textContent = lang === 'fr'
        ? 'Erreur lors de l\'envoi. Veuillez réessayer.'
        : lang === 'zh'
        ? '发送失败，请重试。'
        : 'Error sending message. Please try again.';
      contactFeedback.classList.add('visible', 'error');

      setTimeout(() => {
        contactFeedback.classList.remove('visible');
      }, 5000);
    } finally {
      submitBtn.disabled = false;
      submitBtn.classList.remove('loading');
    }
  });
</script>
