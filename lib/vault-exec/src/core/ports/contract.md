# core/ports contract

## Inputs

- Provider request payloads from `core` orchestrators.

## Outputs

- Typed provider responses and deterministic error surfaces.

## Invariants

- Port contracts stay minimal and versionable.
- Port signatures are provider-agnostic.
- No hidden side effects in contract definitions.
