/**
 * Compatibility facade for legacy imports.
 * New code should import from `../application/phases/edge-to-vertex.ts`.
 */
export { runEdgeToVertexPhase } from "../application/phases/edge-to-vertex.ts";
