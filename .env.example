# ===================
# Server Ports
# ===================
FRESH_PORT=8081      # Fresh Dashboard (dev)
PORT_API=3003        # API Server (dev)

# ===================
# AgentCards Database & Workflows
# ===================
# Dev: relative path to local dev DB
# Prod: absolute path (see .env.production)
AGENTCARDS_DB_PATH=.agentcards-dev.db
AGENTCARDS_WORKFLOW_PATH=config/workflow-templates.yaml

# ===================
# Sentry Configuration
# ===================
# Get your DSN from https://sentry.io/settings/projects/
SENTRY_DSN=https://your-dsn@your-org.ingest.sentry.io/your-project-id

# Environment (development, staging, production)
SENTRY_ENVIRONMENT=development

# Release tracking (auto-detected from git if not set)
# SENTRY_RELEASE=agentcards@v1.0.0

# Traces sample rate (0.0 to 1.0)
# 1.0 = 100% of transactions (use in dev)
# 0.1 = 10% of transactions (recommended for production)
SENTRY_TRACES_SAMPLE_RATE=1.0

# ===================
# GitHub OAuth (Cloud Mode Only)
# ===================
# Required for multi-tenant authentication
# Without these, AgentCards runs in local mode (no auth)
# GITHUB_CLIENT_ID=your_github_oauth_client_id
# GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# GitHub OAuth App Setup:
# 1. Go to GitHub → Settings → Developer settings → OAuth Apps
# 2. Create new OAuth App
# 3. Set callback URLs (both for dev + prod):
#    - http://localhost:8081/auth/callback (dev)
#    - https://your-domain.com/auth/callback (prod)
# 4. Copy Client ID and Client Secret here

# ===================
# Monitoring (Docker Compose)
# ===================
# Grafana admin credentials (change these in production!)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme
